# Binaries
bin/
dist/
*.exe
*.exe~
*.dll
*.so
*.dylib
# Anchored so they match the built binaries, not cmd/trackkrd/.
/trackkr-backend
/trackkrd

# Test binary
*.test

# Output of go coverage
*.out
*.cover
# Coverage HTML only. A blanket *.html swallowed the dashboard templates
# once already, and would have taken the extension's pages next; keep the
# rule pinned to the artefact it is actually for.
/coverage.html
/cover.html

# Go workspace
go.work
go.work.sum

# Environment variables
.env
.env.local
.env.*

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

.agents/*

# Claude Code local settings
.claude/
CLAUDE.md
.rtk

tmp
.cache/
.codegraph/
